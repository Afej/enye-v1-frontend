import React from "react";
import "./header.scss";

const Header = () => {
  return (
    <header>
      <h1>Profiles Records</h1>
      <p>Search for a specific profile.</p>
    </header>
  );
};

export default Header;
